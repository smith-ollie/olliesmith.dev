---
import Typography from "./typography/typography";

interface Props {
  value: string;
}

const { value } = Astro.props;
---

<Typography
  variant="heading"
  size="medium"
  className="!leading-[2rem] font-bold relative text-secondary py-2 inline"
  as="h1"
>
  {value}
</Typography>
